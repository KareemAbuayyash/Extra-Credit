# src/main/resources/application.properties

# Application Identity
spring.application.name=employee-payroll-system

# If no profile is specified on the command line:
#   default to "dev", which will pick up application-dev.yml
spring.profiles.active=${ENVIRONMENT_PROFILE:dev}

# (…Swagger/OpenAPI common settings…)
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/api-docs-ui
springdoc.api-docs.enabled=true
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.try-it-out-enabled=true
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.tags-sorter=alpha
springdoc.swagger-ui.filter=true
springdoc.swagger-ui.config-url=/v3/api-docs/swagger-config
springdoc.swagger-ui.url=/v3/api-docs

# Default SMTP (will be overridden by each profile if needed)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=rawang17@gmail.com
spring.mail.password=fswc bscf gnyb iewm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Default logging (unless overridden per profile)
logging.level.com.example.payroll=DEBUG
logging.level.org.springframework.security=DEBUG

# If your app sits behind a load-balancer or reverse proxy:
server.forward-headers-strategy=framework
